# Documentation Agent Instructions

- Ignore the `docs/pdf-files/` directory and all files beneath it.
- Do not open, search, summarize, modify, or use files from `docs/pdf-files/` unless the user explicitly asks for a PDF file or that directory.
- Prefer the Markdown and JSON documentation outside `docs/pdf-files/` as the source of truth.
